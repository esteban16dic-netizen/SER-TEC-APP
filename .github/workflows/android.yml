name: Build APK
on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build with Buildozer
        # Volvemos a la oficial pero con una versión estable
        uses: kivy/buildozer-action@v1
        with:
          buildozer_version: stable
          python_version: 3.12
