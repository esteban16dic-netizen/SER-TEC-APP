name: Build APK
on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build with Buildozer
        # Esta es la dirección oficial completa que SIEMPRE encuentra:
        uses: Enenkel/buildozer-action@v0.0.3
        with:
          buildozer_version: stable
          python_version: 3.12

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SER-TEC-App
          path: bin/*.apk
